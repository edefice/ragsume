# Anthropic API key — used for Claude generation (structured RAG answers).
# https://console.anthropic.com/settings/keys
ANTHROPIC_API_KEY=

# Voyage AI API key — used for embeddings (ingest + query time).
# https://dash.voyageai.com/api-keys
VOYAGE_API_KEY=
